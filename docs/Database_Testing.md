# RaceDay Database Testing

The RaceDay database script was tested successfully using SQL Server Management Studio.

## Verification Queries

SELECT * FROM dbo.[User];
SELECT * FROM dbo.UserProfile;
SELECT * FROM dbo.Event;
SELECT * FROM dbo.Category;
SELECT * FROM dbo.Enrolment;
SELECT * FROM dbo.Result;

## Result

The database script executed successfully without errors and the sample data was displayed correctly.